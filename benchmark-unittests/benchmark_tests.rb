require 'benchmark'

module BenchmarkTests

  def self.main(*arguments)
    # run_benchmark_n_times(count) { |c| benchmark_ironruby c }
  end

  def self.run_benchmark(*arguments)
#   ARGV[0].to_i.times do |i|
#     yield 10 ** i
#   end
  end

# def run_benchmark(n, id)
#   ObjectSpace.garbage_collect
#   Benchmark.benchmark("\n --- Test #{id} with parameters --- \n", 30) do |x|
#     yield x
#   end
# end

end
